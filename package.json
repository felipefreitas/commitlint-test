{
  "name": "commitlint-test",
  "version": "3.0.0-felipe",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "semantic-release": "^17.4.2"
  },
  "release": {
    "branches": [
      "main",
      {
        "name": "dev",
        "prerelease": true
      }, {
        "name": "hml",
        "prerelease": true
      }
    ],
    "plugins": [
      "@semantic-release/commit-analyzer"
    ]
  }
}
